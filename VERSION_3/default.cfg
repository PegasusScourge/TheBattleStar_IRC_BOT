//Channel separated by ',' 

// Caps sensitive
auto_join = #TheBattleStar_admin,#TheBattleStar_blocked,#ONI_clan

// Caps sensitive
blocked_users = 

// Must be all lower case
locked_channels = #thebattlestar_blocked

// Must be all lower case
admin_channels = #thebattlestar_admin

log_channels = #thebattlestar_admin
